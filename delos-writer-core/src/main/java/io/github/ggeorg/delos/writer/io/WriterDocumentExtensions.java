package io.github.ggeorg.delos.writer.io;

/**
 * File extensions owned by the Writer document family.
 */
public final class WriterDocumentExtensions {
    /** Native Delos Writer ZIP package. */
    public static final String DOCUMENT = ".dlw";

    /** Legacy plain-XML extension used before the native package foundation. */
    public static final String LEGACY_DELOS_XML = ".delos";

    /** Legacy plain-XML extension used by the interim Writer format. */
    public static final String LEGACY_DWRITE_XML = ".dwrite";

    private WriterDocumentExtensions() {
    }
}
