/**
 * Platform-neutral rendering model and Writer page renderer components.
 *
 * <p>This package draws to {@code RenderTarget}; JavaFX Canvas, PDF, SVG, and
 * test recorders should be adapters outside the core renderer implementation.</p>
 */
package io.github.ggeorg.delos.writer.render;

import io.github.ggeorg.delos.render.RenderTarget;
