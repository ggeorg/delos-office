<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.ggeorg</groupId>
        <artifactId>delos</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>delos-writer-render</artifactId>
    <name>Delos Writer Render</name>
    <description>JavaFX-free Writer page rendering pipeline shared by screen and PDF outputs.</description>

    <dependencies>
        <dependency>
            <groupId>io.github.ggeorg</groupId>
            <artifactId>delos-render</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.ggeorg</groupId>
            <artifactId>delos-writer-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.ggeorg</groupId>
            <artifactId>delos-writer-layout</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
